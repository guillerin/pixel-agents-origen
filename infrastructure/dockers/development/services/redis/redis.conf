# Token Town — Development Redis configuration

# Persistence
save 60 1
save 300 10

# Memory
maxmemory 128mb
maxmemory-policy allkeys-lru

# Logging
loglevel notice

# Security (development only — no password)
protected-mode no
